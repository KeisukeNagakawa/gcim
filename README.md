# gsim
Fastest way for git-add and commit with message


# installation
 (for mac for now)

## 1.permission
```
chmod +x gsim
```

## 2. path
add  `export PATH=$PATH:~/dir_to_this_repository_cloned` to your rc file(bashrc, zshrc)


Pull request is always welcome.
